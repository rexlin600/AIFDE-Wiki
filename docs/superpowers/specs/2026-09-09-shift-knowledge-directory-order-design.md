# 知识库目录序号后移设计

将 A–P 知识库目录整体后移四个字母，变为 E–T：`A-用户指南` → `E-用户指南`，依此类推，`P-就业市场` → `T-就业市场`。`Z-模板` 与 `Z-资源` 保持不变。仅更新现行 Markdown 链接与本地 Obsidian 工作区中受影响的路径；不修改正文或标题，不暂存既有 `.gitignore` / `.obsidian` 改动。由于目标字母与现有目录重叠，必须先移动至临时目录再移动至最终目录。
