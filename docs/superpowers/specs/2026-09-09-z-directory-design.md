# Z 类目录命名设计

将 `90-Templates` 重命名为 `Z-模板`，将 `99-Assets` 重命名为 `Z-资源`，使它们在 A–P 知识库目录之后统一排序。更新 Vault 中的现行 Markdown 引用及 Obsidian 模板/附件目录设置；不修改 `docs/superpowers/` 中的历史过程记录。当前未提交的 `.obsidian/app.json` 改动仅更新其附件目录字段，且不随本次提交暂存。
