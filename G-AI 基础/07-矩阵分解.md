---
type: concept
domain:
  - AI Foundations
depth: L2
importance: common
maturity: draft
created: 2026-09-09
updated: 2026-09-09
last_verified: 2026-09-09
aliases:
  - Matrix Decomposition
tags:
  - foundations
  - mathematics
  - matrix-decomposition
---

# 矩阵分解

## 一句话定义

矩阵分解（Matrix Decomposition）把一个矩阵写成若干结构更简单的矩阵之积，以揭示主要方向、独立尺度和低秩结构。

## 本篇范围

本篇重点解释特征值、特征向量、特征分解、奇异值分解（SVD）和低秩近似。数值求解算法和大规模分布式分解不展开。

## 前置知识

需要理解[线性代数](01-线性代数.md)中的矩阵乘法、向量长度和正交方向。

## 它解决 AI 中的什么问题

词表 Embedding、用户物品交互和模型权重都可能形成巨大矩阵。矩阵分解判断这些矩阵是否主要由少数方向构成，从而支持降维、去噪、压缩和低秩适配。

## 不理解会遇到什么问题

- 只看重构误差选择压缩秩，忽略检索或分类质量；
- 把特征分解用于任意非方阵；
- 显式计算 $A^\mathsf{T}A$，放大条件数和舍入误差；
- 把最大方差方向直接解释成最重要的业务语义。

## 它在 AI 链路中的位置

上游是样本表示、Embedding 或训练后的权重矩阵；本主题提取主要方向并构造低秩近似；下游以更低维表示或更小权重完成检索、训练和推理。

## 从 Embedding 矩阵压缩开始

设 $E\in\mathbb R^{V\times d}$ 是词表 Embedding，$V$ 是 Token 数量，
$d$ 是隐藏维度。若 $E$ 的有效秩远小于 $d$，可用
$E_k=U_k\Sigma_kV_k^\mathsf{T}$ 近似它。保存两个较小因子能够减少参数，
但必须验证语义相似度和下游任务是否保持。

## 特征值和特征向量

对方阵 $A\in\mathbb{R}^{n\times n}$，若存在非零向量 $v$ 和标量 $\lambda$ 使

$$
Av=\lambda v
$$

则 $v$ 是特征向量，$\lambda$ 是对应特征值。一般向量经过 $A$ 后方向会改变，特征向量是方向保持不变、只被缩放的特殊方向。

要寻找非零 $v$：

$$
(A-\lambda I)v=0
$$

齐次线性方程存在非零解，要求 $A-\lambda I$ 不可逆，因此

$$
\det(A-\lambda I)=0
$$

这个特征方程先给出 $\lambda$，再代回求 $v$。

### 手算示例

$$
A=\begin{bmatrix}2&0\\0&1\end{bmatrix}
$$

对 $e_1=[1,0]^\mathsf{T}$，$Ae_1=2e_1$；对
$e_2=[0,1]^\mathsf{T}$，$Ae_2=e_2$。矩阵沿第一坐标轴放大 2 倍，
沿第二坐标轴保持不变。

## 特征分解

若 $A$ 有 $n$ 个线性无关特征向量，把它们作为列组成

$$
V=[v_1\ \cdots\ v_n]
$$

再令 $\Lambda$ 为对应特征值组成的对角矩阵，则 $AV=V\Lambda$。若 $V$ 可逆：

$$
A=V\Lambda V^{-1}
$$

并非所有矩阵都能在实数域做这种分解。实对称矩阵性质最好，可以取正交特征向量，使 $V^{-1}=V^\mathsf{T}$。

## 奇异值分解

任意实矩阵 $A\in\mathbb{R}^{m\times n}$ 都存在分解

$$
A=U\Sigma V^\mathsf{T}
$$

- $U$ 的列是左奇异向量，表示输出空间的正交方向；
- $V$ 的列是右奇异向量，表示输入空间的正交方向；
- $\Sigma$ 的对角元素 $\sigma_1\geq\sigma_2\geq\cdots\geq0$ 是奇异值。

变换可以理解为三步：$V^\mathsf{T}$ 旋转或反射输入坐标，$\Sigma$ 沿各正交轴缩放，$U$ 再旋转或反射到输出坐标。

因为

$$
A^\mathsf{T}A=V\Sigma^\mathsf{T}\Sigma V^\mathsf{T}
$$

$V$ 的列是 $A^\mathsf{T}A$ 的特征向量，特征值为 $\sigma_i^2$。这连接了特征分解与 SVD。

## 低秩近似

SVD 还可以写成秩一矩阵之和：

$$
A=\sum_{i=1}^{r}\sigma_i u_i v_i^\mathsf{T}
$$

$r$ 是矩阵秩。只保留前 $k$ 项：

$$
A_k=\sum_{i=1}^{k}\sigma_i u_i v_i^\mathsf{T}
$$

由于 $u_i$ 与 $v_i$ 分别正交，各秩一成分在 Frobenius 内积下也正交，因此丢弃部分的平方误差为

$$
\lVert A-A_k\rVert_F^2=\sum_{i=k+1}^{r}\sigma_i^2
$$

要在只能保留 $k$ 个正交成分时使误差最小，自然应保留最大的 $k$
个奇异值。Eckart-Young-Mirsky 定理进一步保证，$A_k$ 是所有秩不超过
$k$ 的矩阵中 Frobenius 范数误差最小的近似。

对 Embedding 矩阵而言，$u_i$ 描述 Token 在第 $i$ 个主要方向上的权重，
$v_i$ 描述该隐藏方向，$\sigma_i$ 表示这组结构在矩阵中的强度。截断 SVD
丢弃的是矩阵能量较小的方向，不是已经证明对任务无用的方向。

## NumPy 实验

```python
import numpy as np

embeddings = np.array([[3.0, 2.0, 2.0], [2.0, 3.0, -2.0]])
U, singular_values, Vt = np.linalg.svd(embeddings, full_matrices=False)
reconstructed = U @ np.diag(singular_values) @ Vt

rank_one = singular_values[0] * np.outer(U[:, 0], Vt[0, :])
rank_one_error = np.linalg.norm(embeddings - rank_one, ord="fro")

assert np.allclose(reconstructed, embeddings)
assert np.isclose(rank_one_error, singular_values[1])
print("embedding singular values:", singular_values)
print("rank-one reconstruction error:", rank_one_error)
```

完整 SVD 在浮点容差内重构原矩阵；只保留一个奇异方向时，误差等于被丢弃奇异值的平方和开根号。

## 工程排错

| 现象 | 可能原因 | 优先检查 |
| --- | --- | --- |
| 压缩后重构误差小但任务变差 | 被丢弃方向对任务重要 | 按秩同时评估任务指标和重构误差 |
| PCA 方向被整体均值主导 | 输入数据未中心化 | 检查训练与推理是否使用同一均值 |
| 小奇异值方向产生巨大参数 | 对近零奇异值求逆 | 设置截断或正则化并检查条件数 |
| 分解结果跨运行方向翻转 | 奇异向量符号不唯一 | 比较子空间或重构结果而非符号 |
| 大矩阵分解内存不足 | 使用完整 SVD | 评估截断、随机化或增量算法 |

PCA 通常对中心化数据矩阵做 SVD。截断秩 $k$ 是质量、存储和计算成本之间的超参数，必须在下游指标上验证。

## 公式看不懂时

SVD 像是在寻找矩阵最擅长拉伸的几组互相垂直的方向。奇异值越大，该方向携带的矩阵能量越多；丢弃很小的方向可以压缩数据。

## 适用与不适用场景

矩阵分解适合线性降维、压缩、去噪和理解线性变换。非线性流形、离散规则或强时间结构未必能由少量线性方向表达。

## 常见误区

- **特征分解适用于任意矩阵。** 一般非方阵不能做普通特征分解，但可以做 SVD。
- **最大奇异值对应最重要业务特征。** 它只表示矩阵能量或方差大，业务价值仍需外部验证。
- **保留更多分量永远更好。** 重构误差会降低，但存储、速度、噪声和下游泛化可能变差。
- **矩阵近似误差等于任务误差。** Frobenius 重构误差与分类、检索等业务指标并不相同。

## 学习收益

学完后，你应该能够解释 PCA、Embedding 压缩和低秩权重的共同结构；用 SVD 构造低秩近似；根据奇异值、重构误差和任务指标选择并排查压缩方案。

## 如何给别人讲清楚

1. 用二维拉伸解释特征方向。
2. 说明特征分解对矩阵有条件，而 SVD 对任意矩阵存在。
3. 把 SVD 拆成旋转、缩放、再旋转。
4. 用秩一矩阵之和解释截断和误差。
5. 连接 PCA、推荐和模型压缩。

## 自检问题

1. 特征向量为什么不能是零向量？
2. 特征分解和 SVD 的适用矩阵有什么不同？
3. $A^\mathsf{T}A$ 的特征值与 $A$ 的奇异值有什么关系？
4. 为什么截断 SVD 优先保留较大奇异值？
5. 重构误差很小为何仍不能保证下游模型表现不变？

## 相关概念

- [线性代数](01-线性代数.md)提供矩阵、正交和范数基础。
- [数值稳定性](05-数值稳定性.md)解释小奇异值和条件数为何影响求解。
- [信息几何](13-信息几何.md)在概率分布空间中使用局部几何结构。

## 资料来源

- Gilbert Strang, *Linear Algebra and Learning from Data*,
  Wellesley-Cambridge Press, 2019。
- Gene H. Golub and Charles F. Van Loan,
  *Matrix Computations*, 4th Edition,
  Johns Hopkins University Press, 2013。
- NumPy Developers, [numpy.linalg.svd](https://numpy.org/doc/stable/reference/generated/numpy.linalg.svd.html)，访问日期：2026-09-09。
